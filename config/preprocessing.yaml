models:
  provider: mock
  anthropic:
    model: claude-opus-4-8
  minimax:
    model: MiniMax-M2.5-highspeed
retry:
  maxAttempts: 3
  backoffSeconds: 2
concurrency:
  default: 4
prompts:
  root: prompts
