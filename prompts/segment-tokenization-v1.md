## Task

Tokenize the segment text below for a Classical Chinese reader. Each token is a contiguous substring with character offsets relative to the segment text (0-based start, end exclusive). Tokens must partition the full segment text without gaps or overlaps.

promptVersion: segment-tokenization-v1

Segment text:
{{segment_text}}

SEGMENT_ID: {{segment_id}}
INPUT_HASH: {{input_hash}}
